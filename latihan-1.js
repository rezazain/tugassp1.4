function hitungLuasPersegiPanjang(panjang, lebar) {
  var luasPersegiPanjang;
  var total;

  luasPersegiPanjang = panjang * lebar;

  total = panjang * lebar;

  document.write("Hasil Luas Persegi Panjang Adalah : ", total);

  return total;
}
console.log(hitungLuasPersegiPanjang(5, 8));
